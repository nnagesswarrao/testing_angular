import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bible.churches',
  appName: 'Basic_bible',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
